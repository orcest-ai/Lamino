# Path on your host where AnythingLLM data will persist.
# This directory will contain the SQLite DB, vectors, and uploaded files.
STORAGE_LOCATION=./storage

# This path is used by AnythingLLM in the container.
STORAGE_DIR=/app/server/storage

# If you host the container remotely, set this to your reachable API URL.
# Example: VITE_API_BASE=http://203.0.113.10:3001/api
# VITE_API_BASE=

# Optional hardening:
# DISABLE_SWAGGER_DOCS=true
# DISABLE_TELEMETRY=true
# DISABLE_VIEW_CHAT_HISTORY=1

# Example provider defaults - set these based on your LLM stack.
# LLM_PROVIDER=openai
# OPEN_AI_KEY=sk-...
# OPEN_MODEL_PREF=gpt-4o
#
# VECTOR_DB=lancedb
# EMBEDDING_ENGINE=native
# WHISPER_PROVIDER=local
# TTS_PROVIDER=native
