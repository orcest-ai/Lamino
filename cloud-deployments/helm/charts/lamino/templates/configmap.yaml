apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "lamino.labels" . | nindent 4 }}
  name: {{ include "lamino.fullname" . }}-config
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}